# 《烬境旅团》开发 TODO

> 最后更新：2026-07-31｜当前阶段：M1 可玩灰盒｜维护者：项目开发者与 Codex

## 使用规则

- `[x]` 已完成并验证；`[ ]` 尚未完成；带“验证中”的任务必须在 Creator 和目标环境运行后才能勾选。
- 按列表顺序推进，优先完成 `P0`。未经评审，不提前实现后续版本功能。
- 每项任务完成时同步更新相关 PRD、TDD、配置说明和测试。
- 遇到需求不明确时记录到“待决策”，不要由代码自行假设。

## M0：立项与工程基线

- [x] `P0` 确定名称：《烬境旅团》/ Ashrealm Brigade。
- [x] `P0` 确定技术栈：Cocos Creator 3.8.8 + TypeScript。
- [x] `P0` 确定微信基础库最低兼容版本：2.12.0。
- [x] `P0` 统一 Git 仓库并推送 `main`。
- [x] `P0` 设置竖屏设计分辨率：750 × 1334。
- [x] `P0` 创建并验证 `Boot.scene`。
- [x] `P0` 创建代码式启动页和 `Bootstrap` 组件。
- [x] `P0` 集中管理首批战斗、英雄与离线经济参数。
- [x] `P0` 锁定并验证屏幕适配策略与安全区规则。
- [x] `P0` 配置 ESLint、Prettier、Vitest 和标准命令。
- [ ] `P1` 建立配置校验与数值模拟工具。

## M1：核心战斗灰盒

- [x] `P0` 验证启动页进入灰盒战斗。
- [x] `P0` 验证点击攻击、自动攻击、击杀和金币奖励。
- [x] `P0` 验证英雄升级费用与伤害增长。
- [x] `P0` 将伤害公式拆为可测试的 `DamageCalculator`。
- [x] `P0` 实现普通怪物刷新逻辑并通过单元测试。
- [x] `P0` 实现并验证第 10 关 Boss、倒计时、胜利、失败和重新挑战。
- [x] `P0` 为伤害、关卡推进和 Boss 状态建立 7 个单元测试。
- [x] `P0` 实现并验证战斗暂停、恢复和前后台切换。
- [ ] `P1` 增加伤害数字、受击反馈和临时战斗动画。
- [ ] `P1` 建立对象池，管理伤害数字和临时特效。
- [x] `P0` 完成首轮浏览器灰盒回归。

## M2：存档与成长闭环

- [x] `P0` 定义 `SaveData` V1 和默认存档。
- [x] `P0` 实现 Web/微信统一的 `PlatformAdapter` 接口与 Cocos 平台实现。
- [x] `P0` 实现主存档、临时存档、回读校验和备份恢复。
- [x] `P0` 保存并验证金币、关卡和英雄等级。
- [x] `P0` 验证浏览器刷新后进度正确恢复且不会重复结算。
- [x] `P0` 实现并验证离线时间记录和最高 12 小时收益。
- [x] `P1` 实现存档版本迁移与损坏恢复。
- [x] `P1` 添加并验证开发用清档与存档检查工具。

## M3：MVP 系统

### 英雄与技能

- [x] `P0` 定义并验证 8 位英雄配置 Schema。
- [x] `P0` 实现并验证英雄解锁、升级、属性聚合和 3 个上阵位。
- [x] `P0` 实现并验证主动技能、冷却和最多 4 个技能槽。
- [x] `P1` 实现并验证被动技能及永久属性加成。

### 装备与背包

- [x] `P0` 定义并验证 7 个装备部位、品质和词条 Schema。
- [x] `P0` 实现并验证掉落、物品实例、穿戴与属性比较。
- [x] `P0` 实现并验证背包筛选、排序和史诗以上自动保护。
- [x] `P1` 实现并验证强化、升星、分解、一键穿戴与出售。

### 任务与经济

- [x] `P0` 实现并验证金币来源/消耗记录和资源防负数。
- [x] `P0` 实现并验证每日任务、跨天刷新和幂等奖励。
- [x] `P1` 实现并验证成就累计与领取。
- [ ] `P1` 实现激励广告适配与失败降级。

## M4：页面与内容

- [x] `P0` 完成并验证战斗、英雄、装备、背包、任务灰盒页面。
- [ ] `P1` 完成主城、图鉴、商店和设置页面。
- [ ] `P0` 建立 `heroes`、`monsters`、`stages`、`skills` 配置。
- [ ] `P0` 填充并校验 100 关、20 种怪物和 5 个 Boss。
- [ ] `P0` 填充 8 位英雄、装备模板、词条和掉落表。
- [ ] `P1` 接入正式 UI、Spine、特效、音乐和音效。
- [ ] `P1` 完成红点、空状态、加载和错误状态。

## M5：微信发布

- [ ] `P0` 完成首次微信小游戏空包构建。
- [ ] `P0` 验证基础库 2.12.0 兼容路径。
- [ ] `P0` 规划主包、Asset Bundle 分包和远程资源。
- [ ] `P0` 真机验证启动、存档、前后台、音频和触控。
- [ ] `P0` 验证广告成功、失败、取消和重复回调。
- [ ] `P0` 完成低端机 FPS、内存和加载时间测试。
- [ ] `P0` 完成隐私、版本、错误日志和发布配置检查。
- [ ] `P0` 完成 1～100 关发布候选版本回归。

## 当前待决策

- [ ] 首章目标通关总时长。
- [ ] Boss 分布及 5 个独立 Boss 的关卡位置。
- [ ] 背包初始容量和扩容规则。
- [x] 装备强化采用 MVP 必定成功规则。
- [ ] 钻石与商店是否进入首发范围。
- [ ] 各类激励广告的每日次数上限。

## 当前下一步

1. 定义并校验 20 种普通怪和 5 个独立 Boss 配置。
2. 建立连续 100 关配置，Boss 每 10 关出现并循环使用 5 种 Boss。
3. 将生命、金币、Boss 时限和显示名称从公式占位切换为关卡配置驱动。
4. 保持 100 关后的无限成长公式，并允许 Boss 类型继续循环强化。
5. 增加配置完整性、关卡连续性和 Boss 可达性测试，再进行 Creator 验证。
